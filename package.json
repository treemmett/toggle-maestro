{
  "name": "toggle-maestro",
  "description": "Toggle those features, with finesse",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "lint": "lerna run lint",
    "prepare": "husky install"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "husky": "^8.0.1",
    "lerna": "^6.0.1"
  }
}
